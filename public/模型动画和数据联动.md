	一、垂直阵开机动画
	1、创建C009H_has_currently_playing默认为false,需要执行的模型名称：20new,动画名称：Animation，判断命令码 C009H 数据， 如果 clcdRaw（出揽长度）整数值大于等于0，并且C009H_has_currently_playing为false，那么执行动画正放，并且将第一次的clcdRaw值存储为阈值C009HThreshold用来判断是否执行动画倒放，动画执行完成后存储 C009H_has_currently_playing 为 true 用来判断是否处于垂直阵运行中，开机动画结束后那么 执行创建绳子demo 20fromA（B: liantou001）
	二、垂直阵关机动画逻辑
	1、需要执行的模型名称：20new,动画名称：Animation，判断命令码 C009H 数据， 如果 clcdRaw（出揽长度）小于等于存储的阈值C009HThreshold 并且 C009H_has_currently_playing 为 true  那么移除绳子demo然后执行动画倒放 动画执行完成后将 C009H_has_currently_playing 变成false
	三、垂直阵运行中逻辑
	1、判断命令码 C009H 数据，如果 C009H_has_currently_playing 为 true 且 clcdRaw（出揽长度）整数值大于 C009HThreshold阈值，并且根据 clcdRaw 数值 修改 绳子demo 绳长，垂直阵运行中才显示 cmd:"5206H" 显示的信息牌子
    四、托体阵开机动画判断逻辑
    1、创建C011H_has_currently_playing默认为false,需要执行的模型名称：donghua02new,动画名称：donghua02_first，判断命令码 C011H 数据， 如果 tlsfcdRaw（出揽长度）整数值大于等于0，并且C011H_has_currently_playing为false，那么执行动画正放，并且将第一次的tlsfcdRaw值存储为阈值C011HThreshold用来判断是否执行动画倒放，动画执行完成后存储 C011H_has_currently_playing 为 true 用来判断是否处于垂直阵运行中，开机动画结束后那么 执行创建绳子demo 02fromA（B: donghua02-011）
	五、托体阵关机动画判断逻辑
    1、需要执行的模型名称：donghua02new,动画名称：donghua02_first，判断命令码 C011H 数据， 如果 tlsfcdRaw（出揽长度）小于等于存储的阈值C011HThreshold 并且 C011H_has_currently_playing 为 true  那么移除当前绳子demo后执行动画倒放 动画执行完成后将 C011H_has_currently_playing 变成false
	六、托体阵运行中的动画逻辑
    1、判断命令码 C011H 数据，如果 C011H_has_currently_playing 为 true 且 tlsfcdRaw（出揽长度）整数值大于 C011HThreshold 阈值，并且根据 tlsfcdRaw 数值 修改 绳子demo 绳长
	七、接收阵执行开机动画逻辑
    1、判断命令码 C010H 数据，如果 firxzjcztRaw 为 "1" 那么 模型名称：donghua03new 动画名称 Animation 正放动画，如果 firxzjcztRaw 为 "2" 倒放动画，如果 firxzjcztRaw 为 "0"动画停止 同时 模型名称：donghua03new 中 mesh 名称为 lansheng02 的部分隐藏， firxzjcztRaw 为 "1"或者 "2" 的时候 模型名称：donghua03new 中 mesh 名称为 lansheng02 的部分显示  柔性绳子隐藏，firxzjcztRaw 为 "0" 的时候donghua03new 中 mesh 名称为 lansheng02 的部分隐藏 柔性绳子显示 
	八、接收阵执行关机动画逻辑
    1、需要执行的模型名称：donghua01new,动画名称：Animation，判断命令码 C010H 数据， 如果当firxzjcztRaw 为 "0" 且 柔性绳子显示的时候 tlsfcdRaw（出揽长度）小于等于存储的阈值C010HThreshold 并且 C010H_has_currently_playing 为 true  那么移除绳子demo并且执行动画倒放 动画执行完成后将 C010H_has_currently_playing 变成false
	九、接收阵运行中动画逻辑
    1、判断命令码 C010H 数据，如果 C010H_has_currently_playing 为 true 且 tlsfcdRaw（出揽长度）整数值大于 C010HThreshold阈值，根据 tlsfcdRaw 数值 修改 绳子demo 绳长
    十、接收阵托缆开机动画
    创建C010H_has_currently_playing默认为false,需要执行的模型名称：donghua01new,动画名称：Animation，判断命令码 C010H 数据，当firxzjcztRaw 为 "0" 且 柔性绳子显示的时候 如果 tlsfcdRaw（出揽长度）整数值大于等于0，并且C010H_has_currently_playing为false，那么执行动画正放，并且将第一次的tlsfcdRaw值存储为阈值C010HThreshold用来判断是否执行动画倒放，动画执行完成后存储 C010H_has_currently_playing 为 true 用来判断是否处于垂直阵运行中，动画直接结束后 那么 执行创建绳子demo 01fromA（B: donghua01-004）